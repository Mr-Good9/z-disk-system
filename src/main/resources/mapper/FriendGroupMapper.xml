<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.good.zdisksystem.mapper.FriendGroupMapper">
    <select id="getUserGroups" resultType="com.good.zdisksystem.entity.model.FriendGroup">
        SELECT id, user_id, name, sort, create_time, update_time
        FROM friend_group
        WHERE user_id = #{userId}
        ORDER BY sort ASC, create_time DESC
    </select>
</mapper> 