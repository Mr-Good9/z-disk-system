<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.good.zdisksystem.mapper.MessageLikeMapper">

    <insert id="insert">
        INSERT INTO message_like (message_id, user_id)
        VALUES (#{messageId}, #{userId})
    </insert>

    <delete id="delete">
        DELETE FROM message_like
        WHERE message_id = #{messageId} AND user_id = #{userId}
    </delete>

    <select id="select" resultType="com.good.zdisksystem.entity.model.MessageLike">
        SELECT * FROM message_like
        WHERE message_id = #{messageId} AND user_id = #{userId}
    </select>

    <select id="isLiked" resultType="boolean">
        SELECT COUNT(*) > 0 FROM message_like
        WHERE message_id = #{messageId} AND user_id = #{userId}
    </select>

</mapper>
