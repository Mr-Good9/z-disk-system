# 文件上传配置
file:
  upload-path: ${user.dir}/uploads  # 使用相对路径
  max-size: 1024MB  # 最大文件大小
  allowed-types: # 允许上传的文件类型
    - image/*
    - application/pdf
    - application/msword
    - application/vnd.openxmlformats-officedocument.wordprocessingml.document
    - application/vnd.ms-excel
    - application/vnd.openxmlformats-officedocument.spreadsheetml.sheet
    - application/vnd.ms-powerpoint
    - application/vnd.openxmlformats-officedocument.presentationml.presentation
    - text/*
    - application/zip
    - application/x-rar-compressed

spring:
  servlet:
    multipart:
      max-file-size: 1000MB  # 单个文件大小限制
      max-request-size: 1000MB  # 总请求大小限制
  jackson:
    serialization:
      FAIL_ON_EMPTY_BEANS: false

  # RabbitMQ配置
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    # 开启消息确认机制
    publisher-confirm-type: correlated
    publisher-returns: true
    listener:
      simple:
        # 手动确认消息
        acknowledge-mode: manual
        # 每次从队列获取的消息数量
        prefetch: 1

  # Redis配置
  redis:
    host: localhost
    port: 6379
    password:  # 如果有密码，请设置
    database: 0
    timeout: 10000
    lettuce:
      pool:
        max-active: 8
        max-wait: -1
        max-idle: 8
        min-idle: 0

  websocket:
    # WebSocket配置
    enabled: true
    # 允许的源
    allowed-origins: "*"
    # 最大消息大小
    max-text-message-size: 64KB
    # 最大二进制消息大小
    max-binary-message-size: 512KB
    # 添加以下配置
    path: /ws
    heartbeat:
      time: 25s
      timeout: 20000
    max-session-idle-timeout: 60s
    heartbeat-interval: 25s

minio:
  endpoint: http://localhost:9005  # MinIO服务地址
  accessKey: admin           # 访问密钥
  secretKey: 12345678           # 密钥
  bucket: z-disk                  # 存储桶名称

logging:
  level:
    com.good.zdisksystem: DEBUG
    org.springframework.web.socket: DEBUG
    org.springframework.messaging: DEBUG
    com.good.zdisksystem.config: DEBUG

# 大模型AI配置
openai:
  api:
    key: your-openai-api-key
    url: https://api.openai.com/v1/chat/completions

# 或者使用百度文心一言
baidu:
  api:
    key: I5uur8upxJncHv01
    secret: 91dd93933a25455bacc585d60bc8b940
    url: https://open.bigmodel.cn/api/paas/v4/chat/completions

# 智普AI模型配置
zhipu:
  api:
    key: 91dd93933a25455bacc585d60bc8b940.I5uur8upxJncHv01  # 替换为您的智普AI API密钥
    secret: 91dd93933a25455bacc585d60bc8b940.I5uur8upxJncHv01  # 替换为您的智普AI API密钥对应的密钥
    url: https://open.bigmodel.cn/api/paas/v4/chat/completions
    model: glm-4-flash-250414  # 指定使用GLM-4-Flash模型
